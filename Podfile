# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'new-dimibob' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'CardParts'
	pod 'SwiftyJSON', '~> 4.0'    
	pod 'Alamofire', '~> 4.7'
	pod 'SwiftMessages'

  # Pods for new-dimibob

  target 'new-dimibobTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'new-dimibobUITests' do
    # Pods for testing
  end

end
